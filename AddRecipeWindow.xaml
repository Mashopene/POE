<Window x:Class="RecipeManagementApp.AddRecipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New Recipe" Height="450" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Recipe Name:" Margin="0,0,0,5"/>
        <TextBox x:Name="RecipeNameTextBox" Grid.Row="1" Margin="0,0,0,10"/>

        <Button Content="Add Ingredient" Grid.Row="2" Click="AddIngredient_Click" Margin="0,0,0,10"/>

        <ListBox x:Name="IngredientsListBox" Grid.Row="3" Margin="0,0,0,10"/>

        <Button Content="Add Recipe" Grid.Row="4" Click="AddRecipe_Click"/>
    </Grid>
</Window>
